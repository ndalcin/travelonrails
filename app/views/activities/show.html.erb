<div class="container">
  <a href="#" class="previous" data-id="<%=@activity.id%>">&laquo; Previous</a>

  <a href="#" class="next" data-id="<%=@activity.id%>">Next &raquo;</a>


  <div class="container">
    <h4 id="activity_name"><%= @activity.name %></h4>
    <b>Price per person: </b>
      <p id="activity_price">$<%= @activity.price %> </p>
    <b>Activity Type:</b><br>
      <div class="append_types">
        <% @activity.types.each do |type| %>
        <li><i><%= type.name%></i></li>
        <% end %>
      </div>

  </div>

  <!-- <%= link_to "I want to do this", new_activity_path %> -->
</div>
