<h1><%=@activity.name%></h1>
<h2>You can find this activity at ANY destination</h2>

<h3>$<%= @activity.price %> per person</h3>
<h3>Rating: <%= @activity.rating %>/10</h3>
<h3>Click <%= link_to 'here', type_path(@activity.type) %> to see more <%= @activity.type.name.downcase%> activities)</h3>


<% if current_user && !current_user.vacations.empty? %>


  <%= form_for @activity do |f| %>

    <% if @activity.errors.any? %>
      <div class="error_messages">
        <h2>Form is invalid</h2>
        <ul>
          <% @activity.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <%= f.fields_for :vacation_activities, @activity.vacation_activities.build do |ff| %>

      <%= ff.label "Please select one of your existing vacations to add this activity to" %>
      <br><br>
      <%= ff.collection_select :vacation_id, @vacations, :id, :name %>
      <br><br><br>
      <%= ff.label "How many people will be participating?" %>
      <br><br>
      <%= ff.text_field :people %>
    <% end %>
    <br><br>
    <%= f.submit "Add Activity" %>
  <% end %>
<% end %>
