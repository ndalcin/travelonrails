<%= render partial: 'activity_details', locals: {activity: @activity} %>

<% if current_user && !current_user.vacations.empty? %>

  <%= render partial: 'activity_form' %> <!-- fix this tomorrow !!!!! -->

<% end %>
